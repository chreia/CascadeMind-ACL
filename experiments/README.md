# Historical Experiments

This directory contains selected Gemini-only historical variants and ablations from the CascadeMind development process. They are retained for context, not as the recommended public entrypoints.

Use the root scripts and `scripts/run_camera_ready_experiments.py` for reproducible runs. Historical scripts may expect local SemEval data under `data/` and may write outputs to ignored local paths.
